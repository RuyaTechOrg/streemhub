export const metadata = {
    title: {
      template: '%s | settings',
      default: 'Settings',
    },
  }
  
  const settingsLayout = ({ children }) => {
    return <>{children}</>
  }
  
  export default settingsLayout
  