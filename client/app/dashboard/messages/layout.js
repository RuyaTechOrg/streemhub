export const metadata = {
  title: {
    template: '%s | Messages',
    default: 'Messages',
  },
}

const MessagesLayout = ({ children }) => {
  return <>{children}</>
}

export default MessagesLayout
